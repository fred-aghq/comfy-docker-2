# Runtime configuration for the "legacy" ComfyUI instance.
# This instance mirrors the original pre-refactor setup:
#   CUDA 12.8.1 / Python 3.12.3 / PyTorch cu128
#
# These variables are passed into the running container via the service's
# env_file in docker-compose.yml. They are NOT used for Docker Compose
# interpolation, so no prefixing is needed and no merge step is involved —
# edit this file and restart the instance.
#
# Host-side settings (app path, host port) live directly on the service in
# docker-compose.yml. Settings shared by all instances live in .env.

# Command-line switches passed to ComfyUI's main.py at runtime.
#
# --cache-lru 2 bounds ComfyUI's node-output cache to the 2 most recent
# results instead of the default --cache-classic, which caches unchanged node
# outputs for the life of the process and can grow unbounded over a long
# session (a major contributor to host RAM OOMs — see COMFYUI_MEM_LIMIT in
# .env.dist). Drop it for --cache-none if you still see memory growth, or back
# to --cache-classic if you'd rather trade RAM for faster iteration.
COMFY_COMMANDLINE_SWITCHES=--use-sage-attention --cache-lru 2
